# Results directory

Generated CSV tables, raw Monte Carlo RDS chunks and `sessionInfo.txt` are written here. Raw RDS chunks are intentionally ignored by git. Commit final small CSV summaries selectively or archive large outputs in a GitHub release, Zenodo deposit or OSF component.
